import unittest
from app.player_list import PlayerList

# test that the list is and isn't empty
# test that _tail has updated after _head has been inserted
# test that _tail has been inserted
# test that _head has been deleted
# test that _tail has been deleted
# test node deleted based on key
